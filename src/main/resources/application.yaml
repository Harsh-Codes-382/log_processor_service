spring:
  application:
    name: logsentinel-processor

  config:
    import: optional:configserver:http://localhost:8888

  cloud:
    config:
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
